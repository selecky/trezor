import 'package:flutter/material.dart';

class MyCredentialCard extends StatelessWidget {
  const MyCredentialCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
